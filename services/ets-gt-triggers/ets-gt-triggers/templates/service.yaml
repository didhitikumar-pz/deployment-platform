apiVersion: v1
kind: Service
metadata:
  name: {{ include "ets-gt-triggers.fullname" . }}
  labels:
    {{- include "ets-gt-triggers.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ets-gt-triggers.selectorLabels" . | nindent 4 }}
